import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.optirestapp.app',
  appName: 'optirest-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
